import React from "react";
import OrbitProgress from "react-loading-indicators/OrbitProgress";

const OrbitSpinner = () => {
  return (
    <div >
      <OrbitProgress color="white" size={10} text="" textColor="" />
    </div>
  );
};

export default OrbitSpinner;
